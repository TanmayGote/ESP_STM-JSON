#ifndef DESERIALIZE_H
#define DESERIALIZE_H
//int parseJS(char *jsonString) {
//    cJSON *root = cJSON_Parse(jsonString);
//    if (!root) {
//        printf("Failed to parse JSON \r\n");
//        return -1;
//    }
//    // Deserialize and print information
//    cJSON *loc = cJSON_GetObjectItem(root, "LOC");
//    if (loc != NULL && loc->type == cJSON_String) {
//        printf("Loc: %s \r\n", loc->valuestring );
//    } else {
//        printf("Invalid or missing 'Loc' field in JSON \r\n");
//    }
//    cJSON_Delete(root); // Free the allocated memory for the parsed JSON object
//    return 0;
//}
//
//void *mymalloc(unsigned int size) {
//    return malloc(size);
//}
#endif
